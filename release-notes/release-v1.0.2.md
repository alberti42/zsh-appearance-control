# Change log

## Bug fixes

- Fixed a bug occurring in debug mode (`zac debug 1`). It prevented displaying the correct state of internal variables.

## Docs

- Improved the configuration example for WezTerm about how to resolve paths correctly to ensure that `zac` is correctly triggered.
